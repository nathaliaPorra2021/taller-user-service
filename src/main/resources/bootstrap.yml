spring:
  application:
    name: user-service
  cloud:
    config:
      uri: ${URL_CONFIG_SERVICE}
      username:  ${CONFIG_USER}
      password: ${CONFIG_PASSWORD}
  boot:
    admin:
      client:
        url: http://localhost:8086